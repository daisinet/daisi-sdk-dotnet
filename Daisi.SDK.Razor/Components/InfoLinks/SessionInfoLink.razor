<InfoLink Contents="Sessions are short-lived connections between the Consumers and the Hosts, whether through Direct Connect or Fully Orchestrated Connect. There can be multiple requests and inference sessions that are held within a single session." Text="@Text"></InfoLink>
@code {
	[Parameter] public string Text { get; set; } = "Session";
}