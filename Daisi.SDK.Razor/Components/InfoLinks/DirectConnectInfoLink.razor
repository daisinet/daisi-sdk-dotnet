<InfoLink Contents="Direct connect is when a Consumer, after creating an approved session, is granted access to interact directly with a Host. This is faster, as the Orc doesn't have to relay the messages." Text="@Text"></InfoLink>
@code {
	[Parameter] public string Text { get; set; } = "Direct Connect (DC)";
}

